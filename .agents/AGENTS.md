# Behavioral Rules

## Firmware Flashing Protocol
- Always ask the user for explicit permission/approval before running any command that flashes/uploads firmware to the device (e.g., `pio run -t upload` or `pio run -e ... -t upload`).
